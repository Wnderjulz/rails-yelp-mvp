<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content, placeholder: "Ta review" %>
  <%= f.input :rating, placeholder: "Ton rating de 1 à 5" %>
  <%= f.submit "Ajouter", class: "btn btn-primary" %>
<% end %>